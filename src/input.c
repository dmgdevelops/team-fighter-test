#include "simple_logger.h"
#include "input.h"
/*
void tick(InputAction action, Uint32 timestamp,  Uint32[] bufferObj, Uint32[] timeRecord){
    for(int i = 0; i < 89; i++)
    {
        bufferObj[i] = bufferObj[i+1];
        timestamp[i] = timestamp[i+1];
    }
    bufferObj[89] = action;
    timeRecord[89] = timestamp;
}
*/


/*eol@eof*/
